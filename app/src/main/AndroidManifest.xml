<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="robfernandes.xyz.mynews">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".NewsApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:fullBackupContent="@xml/backup_descriptor">
        <activity
            android:name=".ui.activities.MainActivity"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:theme="@style/AppTheme.NoActionBar"
            tools:ignore="InnerclassSeparator">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activities.SearchSelectorActivity"
            android:label="Search"
            android:parentActivityName=".ui.activities.MainActivity"
            tools:ignore="InnerclassSeparator">

            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ui.activities.MainActivity" />
        </activity>
        <activity
            android:name=".ui.activities.AboutActivity"
            android:label="About"
            android:parentActivityName=".ui.activities.MainActivity"
            tools:ignore="InnerclassSeparator">

            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ui.activities.MainActivity" />
        </activity>
        <activity
            android:name=".ui.activities.NotificationsActivity"
            android:label="Notifications"
            android:parentActivityName=".ui.activities.MainActivity"
            tools:ignore="InnerclassSeparator">

            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ui.activities.MainActivity" />
        </activity>
        <activity
            android:name=".ui.activities.HelpActivity"
            android:label="Help"
            android:parentActivityName=".ui.activities.MainActivity"
            tools:ignore="InnerclassSeparator">

            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ui.activities.MainActivity" />
        </activity>
        <activity
            android:name=".ui.activities.NewsDisplayActivity"
            android:label="NewsDisplay"
            android:parentActivityName=".ui.activities.MainActivity"
            tools:ignore="InnerclassSeparator">

            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ui.activities.MainActivity" />
        </activity>
        <activity
            android:name=".ui.activities.SearchDisplayActivity"
            android:label="SearchDisplay"
            android:parentActivityName=".ui.activities.SearchSelectorActivity"
            tools:ignore="InnerclassSeparator">

            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ui.activities.SearchSelectorActivity" />
        </activity>

        <receiver android:name=".receiver.AlarmManagerReceiver" />
    </application>

</manifest>